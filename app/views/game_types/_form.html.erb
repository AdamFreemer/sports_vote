<%= form_with(model: game_type, local: true) do |form| %>
  <% if game_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_type.errors.count, "error") %> prohibited this game_type from being saved:</h2>

      <ul>
      <% game_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :game_type_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
