<%= form_with(model: play_type, local: true) do |form| %>
  <% if play_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(play_type.errors.count, "error") %> prohibited this play_type from being saved:</h2>

      <ul>
      <% play_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :play_type_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
