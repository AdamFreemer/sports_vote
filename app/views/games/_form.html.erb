<%= form_with(model: game, local: true) do |form| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :game_name %>
  </div>

  <div class="field">
    <%= form.label :game_title %>
    <%= form.text_field :game_title, id: :game_game_title %>
  </div>

  <div class="field">
    <%= form.label :team_one %>
    <%= form.text_field :team_one, id: :game_team_one %>
  </div>

  <div class="field">
    <%= form.label :team_two %>
    <%= form.text_field :team_two, id: :game_team_two %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
